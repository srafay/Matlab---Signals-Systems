i.
SELECT ENAME, DEPTNO
FROM EMP
WHERE EMPNO=7566;

ii.
SELECT ENAME, DEPTNO
FROM EMP
WHERE DEPTNO=10 OR DEPTNO=30
ORDER BY ENAME;

iii.
SELECTENAME, DEPTNO
FROM EMP
WHERE TO_CHAR(HIREDATE,'YYYY')='1982';

iv.
SELECT ENAME
FROM EMP
WHERE ENAME LIKE %LL% AND DEPTNO=30 OR MGR=7782;

v.
SELECT ENAME FROM EMP
WHERE JOB = CLERK AND (DEPTNO = 10 OR DEPTNO = 20) AND TO_CHAR(HIREDATE,'YYYY')<'1983';

vi.
SELECT ENAME, SAL
FROM EMP
WHERE NOT (SAL BETWEEN 1500 AND 2850);

vii.
SELECT ENAME, SAL, COMM 
FROM EMP
WHERE COMM > SAL*1.10; 